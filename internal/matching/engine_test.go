package matching

import (
	"testing"
)

func TestMatch_매수가가_매도가보다_높으면_체결된다(t *testing.T) {
	me := NewMatchingEngine()
	me.Start()
	me.OrderCh -> &Order{
		sellOrder := &Order{
			
		}
	}
}
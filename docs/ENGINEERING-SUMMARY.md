# GoExchange 엔지니어링 요약

> **기준 시점** — **앱 코드 기준 `82b4d7f`**(29~33번이 모두 이 바이너리로 측정됐다) /
> 측정 하니스 `5c1f657` / **32번 완료(32-A 탐색 · 32-B 확증)** / **33번 완료(정산 비용 상승 원인 진단)** /
> **34번 완료(인덱스 단독 수정·재측정)**.
>
> **34번의 GCP 실행은 `82b4d7f` 이미지 + migration 006 한 레이어를 얹은 파생 이미지로 돌았다**
> — 배포 바이너리 SHA-256은 `82b4d7f`와 동일하고, 런타임 변경은 인덱스 하나뿐이다.
>
> 이 문서의 목적은 **완료된 것과 아직 증명되지 않은 것을 5분 안에 구분**하는 것이다.
> 전체 연대기는 [벤치마크 목록](benchmarks/README.md)과 [리팩토링 로그](refactor/README.md)에 있다.

---

## 1. 문제와 최종 결과

**시작 지점의 장애** — 급등락 스파이크 부하에서 세 가지가 동시에 무너졌다:

- **매달림**: 주문 응답 p95가 **24초**까지 늘어짐(입장 게이트를 느슨하게 뒀을 때)
- **취소 실패**: 취소 시도의 **48.67%가 500**(엔진이 하류에 막혀 취소 커맨드를 못 받음)
- **정산 직렬화**: 종결 이벤트마다 파티션 전체가 멈춰 **N=4 worker pool 평균 활용률 13.13%**
  (**약 0.52 worker-equivalent**)

**현재 (`Server4 / DB8 / Settlement N=8`, 31번 실측)**:

| 항목 | 값 | 근거 |
|---|---|---|
| 응답 가용성 | **100.00%** (1,125,591 / 1,125,591) | [31번](benchmarks/31-2026-07-31-db-instance-scaleup.md) |
| 1초 계약 초과 | **0건** | 〃 |
| 취소 인프라 실패(500) | **0건**, 취소 성공률 100.00% | 〃 |
| 정합성 위반 · fallback · quarantine · duplicate | **전부 0** | 〃 |
| 정산 처리율 (hold) | **644.16 trades/s** | 〃 |
| **업무 성공률**(whole-run) | **32.05%** | 〃 |

**동일한 스파이크 프로파일**(합산 **10,000 VU hold · 20,000 VU burst**)의 **whole-run 업무
성공률이 10.82% → 32.05%** 로 상승했다
([26번](benchmarks/26-2026-07-28-availability-spike-remeasurement.md) → 31번, **×2.96**).

> **이 값은 whole-run 집계다.** 현재 k6 SLI에 stage 태그가 없어
> `ramp + 10,000 hold + 20,000 burst + recovery` 전체가 한 값으로 합산된다.
> **구간별(특히 10,000 VU hold) 업무 성공률은 아직 산출되지 않았다** — 32번의 하니스 변경으로 얻는다.

> **나머지 67.95%는 업무 요청 실패다.** 다만 **인프라 장애나 매달림이 아니라, 시스템을 보호하기
> 위해 의도적으로 빠르게 반환한 과부하 셰딩**(503 + `Retry-After`)이다.
> [23번](benchmarks/23-2026-07-23-availability-spike-ab.md)에서 게이트를 느슨하게 했을 때 p95가
> 24초로 늘어지는 것을 실측했고, 이후 **"업무 성공률을 올리려고 게이트부터 느슨하게 하지 않는다"** 를
> 원칙으로 고정했다.

**100%를 유지하는 부하는 별도로 측정했다** — 위 32.05%는 **일부러 포화시키는** 프로파일의
값이다. [32-B](benchmarks/32-2026-08-01-capacity-boundary-session-b.md)에서 같은 구성으로
**10분 hold 독립 실행**을 세 번 돌렸다.

| # | 합산 VU | hold | 업무 성공률 | 응답 가용성 · 1초 초과 · 취소 · 정합성 | 판정 |
|---|---|---|---|---|---|
| 1 | **750** | 10분 | **93.484%** (셰딩 36,989) | 100% · 0건 · 100% · 전부 0 | **실패** |
| 2 | **500** | 10분 | **100.00000%** (412,169) | 100% · 0건 · 100% · 전부 0 | **통과** |
| 3 | **500** | 10분 | **100.00000%** (412,906) | 100% · 0건 · 100% · 전부 0 | **통과** |

> **이 워크로드와 현재 구성에서 유한 실행으로 검증된 최고 VU = 합산 500** (10분 hold, 2회 반복).
> **34번의 인덱스 수정 이후에도 500이다** — 750은 r1 통과 / r2 실패로 갈렸고, 사전 등록 분기대로
> 평균 없이 탈락시켰다.
> **"최대 용량"이 아니다.** VU는 등록 사용자가 아니라 계속 주문 루프를 도는 **활성 가상 사용자**이고,
> 숫자는 주문 혼합·취소 비율·think time에 종속된다.

**32-A의 "3분 750 통과"는 32-B에서 뒤집혔다.** 750의 셰딩은 **hold 4.2~4.4분에 시작**하므로 3분
hold는 포화 직전에 끝난다. 판정 정의를 **10분 × 2회**로 먼저 고정해 둔 것이 실제로 작동했다.

> **⚠ 이 숫자는 hold 길이와 분리되지 않는다 — 32번이 새로 연 가장 큰 항목이다.**
> 부하가 일정한데도 **작업당 정산 실행 시간이 hold 동안 9.19ms → 23.75ms(2.6배)로 오르고,
> 10분 안에 정상 상태에 도달하지 않는다.** 배치 크기는 2.42→2.69로 평평하니 **파편화는 원인이
> 아니다.** **그 이상은 미확정이다** — 원장 크기·인덱스·WAL·락 경합·job mix 중 어느 것도
> 배제되지 않았다. 500도 **10분에 통과했을 뿐**이고 같은 상승 추세를 보인다
> (**몇 분에 실패하는지도, 더 오래 돌리면 평형점에 도달하는지도 측정하지 않았다**).
> **깨끗한 DB에서 시작한 값**이다.

**가장 먼저 깨지는 것은 업무 성공률이다.** 750 실패 실행에서도 응답 가용성 100% · 1초 초과 0건 ·
취소 100% · 정합성 무결이었다 — 입장 게이트가 **지연을 키우는 대신 빠르게 503으로 셰딩**하는
설계가 그대로 작동한 결과다.

**개선의 출처 분해** — 절반 이상이 코드에서 나왔다:

| 구간 | 업무 성공률 | 무엇을 바꿨나 |
|---|---|---|
| 26번 → 29번 | 10.82% → **22.70%** (**×2.10**) | **코드만.** 하드웨어 동일(DB4/N4) |
| 29번 → 31번 | 22.70% → **32.05%** (**×1.41**) | 하드웨어·설정(DB 4→8 vCPU, N 4→8) |

---

## 2. 핵심 개선 아크

### 2차 — 매달림 제거와 빠른 셰딩

주문 접수와 매칭을 분리하고, 포화 시 **매달리는 대신 503으로 빠르게 거절**하도록 바꿨다.
"가용성 = 유한 시간 안에 응답"으로 정의를 세우고, 느린 2xx를 **불가용으로 세는** SLI를 도입했다.

- 대표: [23번 — 가용성 스파이크 A/B](benchmarks/23-2026-07-23-availability-spike-ab.md)
  (하드 보장 증명, 그리고 **취소 실패 48.7%라는 다음 문제를 드러냄**)

### 3차 — 취소 진행성과 정산 병렬화

엔진이 하류(`ExecutionCh`)에 막히면 취소 우선 스케줄링도 무력해지는 구조를 고쳤다
(**하류 인지 게이트** — 0.75 워터마크에서 유입 억제). 정산은 파티션 디스패처 + 전역 워커 풀로
병렬화했다 — **정산 DB 커밋은 병렬로 유지하되, trade 브로드캐스트만 `batchSeq` reorder
coordinator가 디스패치 순서로 방출**한다(프론트가 수신 순서대로 렌더링하기 때문).

- 대표: [26번 — 재측정](benchmarks/26-2026-07-28-availability-spike-remeasurement.md)
  (**취소 인프라 실패 48.67% → 0.00%**)

### 4차 축 1 — per-order fence, durable defer, 병목 이동

종결 이벤트가 **파티션 전체를 멈추던 배리어**를 **주문 단위 fence**로 좁혔다. 실행할 수 없는
terminal은 **내구 기록으로 인계**해 온라인 복구가 가능하게 했다(재시작을 복구 수단으로 삼지 않음).

- [29번](benchmarks/29-2026-07-30-per-order-fence-gcp-remeasurement.md): **worker busy 13.13% → 99.95%**, 처리율 ≈109 → ≈372 trades/s
- [30번](benchmarks/30-2026-07-31-settlement-concurrency-sweep.md): **N=8 기각** — 처리율은 +26.9%였으나 1초 계약 초과 568건
- [31번](benchmarks/31-2026-07-31-db-instance-scaleup.md): DB 4→8 vCPU로 **568건 → 0건**, N=8 회수. 병목이 **DB CPU → 서버 CPU**로 이동

> **측정으로 자기 가설을 뒤집은 기록**이 이 프로젝트의 방법론적 핵심이다:
> 28번의 "배리어가 배치 누적을 가로막는다"를 29번이 **반증**했고(배치가 그대로였다),
> 30번이 기각한 N=8을 31번이 **조건을 바꿔 회수**했다.

---

## 3. 현재 구성에서 검증된 계약 충족 결과

> **31번의 유한 부하 1회에서 관측한 결과**다. 영구적 보장이 아니라 **그 측정 조건에서 계약을
> 충족했다**는 뜻이다.

| 계약 | 정의 | 최신 수치 | 근거 |
|---|---|---|---|
| **응답 가용성** | (2xx ∪ 503) **∧** `duration ≤ 1,000ms`. **느린 2xx는 불가용** | **100.00%** | 31번 |
| **취소 성공률** | 200 / (200 + 인프라 실패). 404·409(정상 경쟁) 제외 | **100.00%**, 500 **0건** | 31번 |
| **업무 성공률**(whole-run) | 2xx 비율 (= 1 − 셰딩률) | **32.05%** | 31번 |
| **정합성** | 리컨실리에이션(ledger_wallet·asset_conservation) 4항목 | **전부 0** — 두 실행 각각 완주 확인 | 31번 |
| **정산 fallback** | `settlement_batch_fallbacks_total` | **0** | 29·30·31번 |
| **quarantine 잔존** | 내구 기록 실패로 terminal이 금지된 주문 | **0** | 29·30·31번 |
| **중복 terminal** [^dup] | `settlement_duplicate_terminal_total` | **0** | 29·30·31번 |

[^dup]: **전역 증명이 아니다.** 이 카운터는 **dispatcher의 waiting queue에서 걸리는 중복만** 센다.
첫 terminal이 이미 dispatch된 뒤 도착하는 중복은 waiting이 비어 있어 잡히지 않는다.
저장소 수준 탐지는 범위 밖으로 남겼다.

**정합성이 비협상인 이유**: 돈이 오간다. 그래서 모든 판정에서 **정합성 게이트를 성능 수치보다
먼저** 통과시키고, 하나라도 깨지면 **성능 수치를 읽지 않는다**. 30번의 N=8 기각이 그 규칙의 실제 적용례다.

---

## 4. 아직 모르는 것

| 항목 | 상태 |
|---|---|
| **고정 부하에서 정산 비용이 오르는 원인** | **지배적인 항 하나를 찾았다(33번).** `trades.buy_order_id`에 인덱스가 없어 `SumBuyerFeesByBuyOrderID`(=`CompleteMarketOrder` 전용)가 `trades`를 **매번 전수 스캔**한다. 크기 축만 움직였을 때 **`market_terminal`만 4.9~6.3배**, batch·cancel은 분산 안에서 평평. `full/N8`에서 **DB 실행시간의 86%**. **N=1에서도 같은 기울기 → 행 락·동시성 경합이 아니다**(NOT granted 최대 1, deadlock 0). **다만 로컬 선별 실험이라 WAL·스토리지·autovacuum 원인은 기각하지 못했고, 이것이 유일한 원인이라고 말할 수 없다.** [33](benchmarks/33-2026-08-02-settlement-cost-growth-diagnostic.md) · **[34에서 수정·재측정 완료](benchmarks/34-2026-08-18-buy-order-index-remeasurement.md)** — 인덱스 추가 후 로컬 기울기가 **4.885/5.724 → 0.998/1.077**, `full/N8` seq 튜플 읽기 **85,049,132 → 0**, 대상 쿼리 비중 **86.1% → 0.9%**. 같은 750 VU에서 셰딩 **36,989 → 0(r1)/2,024(r2)**, `job_growth` **2.58 → 1.256/1.462**. **그래도 750에서 growth가 1.26~1.46으로 남아 있어 다른 항은 여전히 미분리다** |
| **정상 상태의 존재 여부** | **미확정.** 확정된 것은 **"10분 안에는 도달하지 않았다"** 까지다. 더 오래 돌리면 평형점에 도달하는지는 측정하지 않았다. **"정상 상태가 존재하지 않는다"고 쓰지 않는다** |
| **500의 실패 시점** | **미측정.** 500은 10분까지만 돌렸다. 후반 기울기를 선형 연장한 값은 있으나 **750에서 상승이 가속했으므로 외삽 시점은 인용하지 않는다** |
| **500~750 사이** | **미측정.** 해상도 250 VU다. 34번에서 750은 **r1 통과 / r2 실패**로 갈렸다 — 이 구성에서 750은 경계에 걸쳐 있고, **왜 실행마다 갈리는지는 미규명**(job mix는 같은데 growth가 1.256 vs 1.462) |
| 10,000 VU hold(합산) | **100% 조건에는 실패 확정** — 셰딩 발생이 확인됨. **단 정확한 hold 구간 업무 성공률은 그 실행에 stage 태그가 없어 미산출이며, 32.05%는 whole-run 값이다**(32번의 hold 수치와 혼동하지 말 것) |
| 배치 파편화 원인 기여도 | **미확정.** 평균 배치가 fence 제거·동시성 2배·DB 증설 **세 번 모두 2.76~2.79에서 안 움직였다.** terminal 경계가 가장 강한 구조적 가설이나 **run-length 분포를 계측하지 않아 기여도는 미확정** |
| 응답 지연 비교 | **p95의 run-to-run 분산이 약 35%**(동일 구성 29번 120.5ms vs 30번 162.7ms). **p95 단독으로 개선·회귀를 주장할 수 없다** |
| 호스트 분산 | DB 머신 타입 변경이 실행 호스트를 바꾸므로 **DB4→DB8 비교에 호스트 분산이 섞여 있다.** 분리하지 않았다 |

---

## 5. 남은 위험

### 축 2 — 매칭 quantum (**미구현**)

**한 주문이 매칭 이벤트 루프를 무제한으로 점유할 수 있다.** 대형 fan-out 주문이 들어오면
그 처리가 끝날 때까지 같은 심볼의 취소가 굶는다. **처리량과 무관한 진행성 반례**이므로
하드웨어를 아무리 키워도 남는다.

- **필수 결정사항 6개가 정리돼 있으나 아직 닫히지 않았다** — 중단된 주문보다 신규 주문이 앞설 수
  있는가 / 중단된 주문을 취소할 수 있는가 / maker가 중간에 취소되면 재개 커서는 / 부분 실행 중
  우선순위 보존 / Done·Cancel·Trade 방출 순서 / 재개 상태에서 프로세스가 죽으면 복구
- **"설계 계약 완료"가 아니다.** 결정사항 목록만 있고 계약은 미확정이며 구현도 없다

### 다음 병목 후보 — **작동점에 따라 신호가 다르다**

**두 작동점을 섞어 읽으면 안 된다.** 어느 것도 병목으로 확정되지 않았다.

| 작동점 | 관측된 신호 | 상태 |
|---|---|---|
| **10,000 hold · 20,000 burst**(포화 프로파일) | 서버 `us+sy` p95 **85**(burst 90)로 **가장 바빴다** | **병목 확정 아님** — 30번에서 DB가 벽일 때는 p95 99였다. 가장 바쁜 것과 벽인 것은 다르다. **서버 증설 미실행** |
| **750**(경계 프로파일, **10분**) | 실패 시점에 **DB CPU median 85 / p95 92**. worker busy는 후반 99.96%로 포화, `outstanding`이 후반의 **99.3%를 `2N`에 붙어** 있었다 | **확정 아님** — worker와 DB가 **동시에** 벽에 가까웠다. 서버는 median 64로 여유가 있어 **서버 증설은 후보가 아니다** |
| ~~750 / 1,000(3분)~~ | ~~N=8 worker pool 단독, DB median 32.5로 여유~~ | **무효** — 3분 관측이라 DB CPU가 아직 오르기 전이었다. **이 근거로 "N=16이 먼저"라고 쓴 32-A의 결론은 취소됐다** |

### 리컨실리에이션 실행시간 급증 (**원인 미확정**)

체결 114,530건 · 사용자 750명에 **약 5초**, 체결 362,654건 · 사용자 4,000명에 **약 17.5분**.
**데이터 증가폭보다 실행시간이 훨씬 크게 늘어나는 현상을 관측**했으나, **두 실행이 통제되지
않았다**(체결 수뿐 아니라 사용자 수·주문 수·오더북 상태가 모두 다르다).

32-B가 하나를 배제했다 — **체결 311,207건 · 사용자 750명 실행이 30초**로 끝났다. 체결은 32-A
계단식의 0.86배인데 시간은 **1/35**이므로 **체결 수 단독 설명은 성립하지 않는다**(사용자 수 쪽이
지배적으로 보인다). **다만 32-B의 세 실행은 사용자 500~750으로 좁아 두 변수를 분리하지는 못했다.**
**원인은 여전히 미확정이며 우선순위 높은 진단 백로그**다.

> **정산 비용 상승과 섞지 않는다.** 리컨실리에이션은 **사용자·지갑별 LATERAL 집계**라는 별도
> 쿼리이고, 정산 경로(INSERT·UPDATE·행 락·COMMIT·WAL)와 비용 구조가 다르다. **독립 진단 항목**이다.

### 백로그 (알고도 남긴 것)

- **terminal-outbox 원자성** — terminal 커밋과 outbox 마킹이 한 트랜잭션이 아니다(크래시 시 멱등 재처리로 흡수)
- **DB per-call timeout 계약 부재** — `statement_timeout`·`lock_timeout`·pool wait 상한이 없어
  **복구 경로의 wall-clock 상한을 주장할 수 없다**
- **입장 워터마크(0.75)의 성격** — 이것은 **burst 수용량·응답 지연·취소 헤드룸의 정책 절충점**이지
  **지속 처리량을 늘리는 수단이 아니다.** 값을 올려도 지속 처리 용량은 늘지 않고 **유한 burst
  흡수량만 달라진다.** 32번이 경계(500)를 확정했으므로 **정산 비용 상승의 원인 규명 뒤 마지막으로
  조정한다** — 비용이 왜 오르는지 모르는 상태에서 워터마크를 만지면 셰딩 시작 시점만 옮긴다

---

## 6. 다음 작업

| # | 작업 | 상태 |
|---|---|---|
| **32-A** | 하니스 변경 + 계단식 탐색 + 독립 실행 정밀화 | ✅ **완료** — 3분 관측(**750 통과는 32-B에서 뒤집힘**) |
| **32-B** | **10분 hold 독립 실행 3회** | ✅ **완료** — **최고 검증 VU = 500**(750은 10분에 실패) |
| **진단 (a)** | **정적 DB 조사** — 인덱스 정의·사용량, autovacuum·dead tuple·checkpoint·WAL, 정산 SQL의 `EXPLAIN (ANALYZE, BUFFERS, WAL)`, `pg_stat_statements` 호출당 시간 | ✅ **완료(33번)** — seq scan이 뜬 정산 읽기 쿼리는 **`SumBuyerFeesByBuyOrderID` 하나**뿐 |
| **진단 (b)** | **실제 정산 함수 스윕** — DB 크기(초기/중간/32-B 종료) × 동시성(**N=1 / N=8**), batch·사용자 분포·주문 유형 고정, **lock wait·WAL**까지 관측 | ✅ **완료(33번)** — 6셀 × 2회. **크기 축이 원인, 동시성은 증폭기** |
| **인덱스 수정** | `trades.buy_order_id` 인덱스 추가 (migration 006, `CREATE INDEX CONCURRENTLY`) | ✅ **완료(34번)** |
| **GCP 재검증** | 수정 후 32-B와 **같은 조건**(10분 hold, 500/750 VU) 재측정 | ✅ **완료(34번)** — 로컬 PASS · 500 PASS · **750 r1 PASS / r2 FAIL**. **최고 검증 용량은 500 유지** |
| **500~750 해상도 좁히기** | 625 VU 등을 10분 × 2회로 측정 | **다음 후보** — 750이 경계에 걸쳐 있음이 34번에서 드러났다 |
| **남은 비용 성장 항** | 인덱스 제거 후에도 750에서 growth 1.26~1.46 — WAL·checkpoint·autovacuum·락 경합 분리 | **미착수** — 33·34 모두 분리하지 않았다 |
| **N=16 ①** | N=16에서 **동일 경계 탐색**(10분 기준, 500이 비교 기준선) | **더 멀어졌다** — 33번이 **동시성 부족을 가리키지 않았다** |
| **N=16 ②** | **포화 프로파일**(10,000 hold · 20,000 burst)에서 **안전 게이트 재검증** | **빼면 안 된다** — 30번에서 N=8이 DB4 포화에서 1초 계약을 깼다 |
| **N=16 채택** | 위 **두 조건을 모두 통과해야** 채택 | — |
| — | 축 2 **계약 6개 확정** 후 quantum 구현 여부 결정 | 계약 문서(구현은 별도 판단) |

**진단 (b)의 판정은 "N=1부터 크기를 탄다" 쪽으로 갈렸다** — `market_terminal`이 크기 축을 따라
**N=1에서 4.9배, N=8에서 5.2~6.3배** 느려지고 `trade_batch`·`cancel_terminal`은 run-to-run 분산
(최대 34%) 안에서 평평하다. `pg_locks` NOT granted 최댓값은 N=1에서 0, N=8에서 1이고 deadlock은 0이라
**락·동시성 경합 가설은 주 원인에서 빠졌다.** N=8은 원인이 아니라 **증폭기**다(full에서 22 → 115ms).

> **⚠ `EXPLAIN`만으로는 닫히지 않는다는 경계는 그대로 유효했다.** 다만 이번에는 읽기 쿼리 하나가
> 실제로 지배적이었다 — `full/N8`에서 `SumBuyerFeesByBuyOrderID`가 **DB 실행시간의 86%**를 차지한다.
> 그래도 판정을 닫은 것은 `EXPLAIN`이 아니라 **크기 × 동시성 스윕**이다.

**증설(N=16·서버 VM)은 더 멀어졌다** — 32-A는 "서버·DB에 헤드룸이 남고 워커만 포화되므로
N=16이 먼저"라고 적었으나, 그 근거는 **3분 관측의 DB CPU median 32.5%** 였다. **10분 실행에서 750이
실패하는 시점의 DB CPU median은 85%(p95 92)** 다. **33번이 그 DB CPU의 상당 부분을 설명할 후보를
찾았으므로**(전수 스캔), 워커를 늘리는 것은 더더욱 답이 아니다. **인덱스 수정 → GCP 재검증이 먼저다.**

> **⚠ 로컬 결과의 해석 범위를 넘기지 않는다.** 기울기가 나왔으므로 **논리적 데이터 크기·SQL 후보를
> 강하게 지지**하지만, **WAL·스토리지·autovacuum 원인을 기각하지는 못한다.** 절대 지연(노트북 Docker)도
> GCP로 이식되지 않는다. **"이것이 32-B 상승의 유일한 원인"이라고 쓰지 않는다.**

---

## 방법론에 대해

이 프로젝트가 지킨 규칙은 셋이다.

1. **안 재고 고치지 않는다.** 모든 수정은 실측이 지목한 병목만 대상으로 한다.
2. **정합성은 비협상.** 성능 수치는 정합성 게이트를 통과한 뒤에만 읽는다.
3. **틀린 것은 기록한다.** 반증된 가설, 기각된 구성, 잘못 잡은 판정 기준을 지우지 않고 남긴다
   (28번 인과 주장 반증, N=8 최초 기각, 29번 주 가설 지표의 부적절성, skew 산출 방법 오류,
   **32-A의 "750 통과"가 32-B에서 뒤집힌 것**).
